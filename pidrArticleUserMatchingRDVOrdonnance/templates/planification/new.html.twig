{# templates/planification/new.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Planification</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_planification_index') }}">back to list</a>
{% endblock %}