{% extends 'baseDoctor.html.twig' %}

{% block title %}Ordonnance{% endblock %}

{% block body %}
<div class="container">
          <div class="page-inner">
            <div class="page-header">

                    {{ include('ordonnance/_form.html.twig', { form: form }) }}
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_ordonnance_index') }}" class="btn btn-outline-light btn-lg shadow-lg rounded-pill px-4 py-2">
                    <i class="bi bi-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
   
{% endblock %}
