{% extends 'base.html.twig' %}

{% block title %}Détail de la Facturation{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-center">💰 Détail de la Facturation</h1>

    <div class="p-4 rounded shadow" style="background-color: #20B2AA;">
        <div class="table-responsive">
            <table class="table table-hover text-center rounded shadow-lg" style="background-color: #87CEEB;">
                <thead class="text-white" style="background-color: #000000;">
                    <tr>
                        <th>Champ</th>
                        <th>Valeur</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th>ID</th>
                        <td><strong>{{ facturation.id }}</strong></td>
                    </tr>
                    <tr>
                        <th>ID Ordonnance</th>
                        <td>{{ facturation.idOrdonnanceId }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{{ facturation.date ? facturation.date|date('Y-m-d') : 'N/A' }}</td>
                    </tr>
                    <tr>
                        <th>Montant</th>
                        <td>{{ facturation.montant }}</td>
                    </tr>
                    <tr>
                        <th>Méthode de paiement</th>
                        <td>{{ facturation.methodePaiement.label }}</td>
                    </tr>
                    <tr>
                        <th>Statut</th>
                        <td>{{ facturation.statut }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="text-center mt-3">
            <a href="{{ path('app_facturation_index') }}" class="btn btn-secondary btn-lg shadow">
                <i class="bi bi-arrow-left"></i> Retour à la liste
            </a>
            <a href="{{ path('app_facturation_edit', {'id': facturation.id}) }}" class="btn btn-warning btn-lg shadow">
                <i class="bi bi-pencil-square"></i> Modifier
            </a>
            <form method="post" action="{{ path('app_facturation_delete', {'id': facturation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette facturation ?');" class="d-inline">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facturation.id) }}">
                <button type="submit" class="btn btn-danger btn-lg shadow">
                    <i class="bi bi-trash"></i> Supprimer
                </button>
            </form>
        </div>
    </div>
</div>
{% endblock %}
